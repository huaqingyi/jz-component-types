export enum AttrInputType {
    // 输入框
    INPUT,
    // 选择器
    SELECT,
    // 上传文件
    UPLOAD,
    // 多个配置 如 border -> [boderleft,....]
    ARRAY,
    // 取色器
    COLORPICKER,
    // boolean 选择
    SWITCH,
}
